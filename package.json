{
  "name": "chatterbox",
  "version": "0.0.0",
  "scripts": {
    "start": "vite",
    "build": "tsc && vite build && vite --config parent-vite.config.js build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.4.4",
    "vite": "^2.7.2"
  }
}
